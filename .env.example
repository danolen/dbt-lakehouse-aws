# Athena Configuration
# Copy this file to .env and fill in your values
# The .env file is ignored by git (so your secrets stay private)

ATHENA_DATABASE=AwsDataCatalog
ATHENA_SCHEMA=dbt_main
ATHENA_REGION=us-east-1
ATHENA_S3_OUTPUT=s3://dn-lakehouse-dev/logs/athena-results/

# AWS Credentials
# These are usually handled by AWS CLI (aws configure)
# But you can also set them here if needed:
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
